name: Validate Blue Team Playbooks

on:
  push:
    branches: [main]
    paths:
      - 'security/blue_team/**'
      - 'security/tools/**'
      - '.github/workflows/validate-playbooks.yml'
  pull_request:
    branches: [main]
    paths:
      - 'security/blue_team/**'
      - 'security/tools/**'
      - '.github/workflows/validate-playbooks.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-playbooks:
    name: Blue Team Playbooks Quality & Link Audit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run Playbooks Validator
        run: |
          cd security
          python tools/validate_playbooks.py
